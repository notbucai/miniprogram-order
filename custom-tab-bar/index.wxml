<van-tabbar active="{{ active }}" bind:change="onChange" wx:if="{{ishide}}">
  <van-tabbar-item wx:for="{{ list }}" wx:key="index" icon="{{ item.icon }}">{{
    item.text
  }}</van-tabbar-item>
</van-tabbar>
